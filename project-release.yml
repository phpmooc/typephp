include: project.yml
optimize: 2
# Release binaries carry their production Composer runtime as embedded opcodes
# and files. Development and PHPT bootstrap builds use project.yml directly.
embedded-files:
  - ./vendor
